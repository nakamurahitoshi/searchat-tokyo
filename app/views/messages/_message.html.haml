.message{"data-messageid": "#{message.id}"}
  .message__info
    .message__username
      = message.user.name
    .message__date
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  .message__body
    - if message.body.present?
      %p.message__text
        = message.body